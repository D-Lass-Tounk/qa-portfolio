version: "3.8"

services:
  jmeter-controller:
    image: justb4/jmeter:5.6.3
    container_name: jmeter-controller
    working_dir: /Work
    volumes:
      # Host (Windows)  →  Container (Linux)
      - E:/qa-portfolio/Projets/Jmeter/JpetStore/Work:/Work
    networks:
      - infra_default
    environment:
      - JVM_ARGS=-Xms1g -Xmx2g

networks:
  infra_default:
    external: true
